# ===================================================================
# ==         COMPREHENSIVE .gitignore for this Projects         ==
# ===================================================================

# 1. Dependencies
# This is the most important one. Never commit your installed packages.
# They are massive and can be recreated with `npm install`.
node_modules/


# 2. Log files
# Log files are generated during runtime and are specific to your local machine.
# They are not part of the source code.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log


# 3. Environment Variables
# NEVER commit files containing secrets, API keys, or database passwords.
# Create a `.env.example` file to show the required variables, but the
# actual `.env` file with real values must be ignored.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local


# 4. Operating System Files
# These are junk files created by macOS, Windows, and Linux.
# They have no place in a source code repository.
.DS_Store
Thumbs.db
desktop.ini


# 5. Build Output / Production Bundles
# If you were using a bundler like Webpack or a compiler like TypeScript,
# you would ignore the generated output directory.
# (Not strictly necessary for *this* project, but essential for others).
/build
/dist
/out


# 6. IDE / Editor-specific files
# Configuration files generated by code editors like VS Code, WebStorm, etc.
# These are personal settings and should not be shared in the project.
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp


# 7. Coverage reports
# Files generated when running tests to see code coverage.
coverage/
.nyc_output/